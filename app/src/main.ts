const recordBar = document.querySelector('.record-indicator span');
const progressBar = document.querySelector('.progress-indicator span');

const mainTitleWord = document.querySelector('.main__title span');
const taskAnswerButtons = document.querySelectorAll('.task-block__answer');
const languageModeButton = document.querySelector('.chenge-language-mode-block__element');